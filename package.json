{
  "devDependencies": {
    "daisyui": "^3.5.0",
    "tailwindcss": "^3.3.3"
  },
  "scripts": {
    "build": "tailwind --config tailwind.config.js --output style/tailwind.css",
    "watch": "tailwind --config tailwind.config.js --output style/tailwind.css --watch --poll"
  }
}
